import styles from "./intent-discovery.style.css?inline";
import row from "./intent-discovery.row.css?inline";
import choice from "./intent-discovery.choice.css?inline";
import layer from "./intent-discovery.layer.css?inline";
import suggestions from "./intent-discovery.suggestions.css?inline";

export const intentDiscoveryStyles = [
    styles,
    row,
    choice,
    layer,
    suggestions
];
